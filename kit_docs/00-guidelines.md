## Build Mandates:
- Do not redesign the architecture without asking.
- Prefer simple Python and Streamlit over extra frameworks.
- Do not refactor unrelated files.
- Build one working slice at a time.
- Every feature must include tests or a reproducible verification path.
- Ask clarifying questions when schema or behavior is ambiguous
- Keep the codebase simple enough to build and extend